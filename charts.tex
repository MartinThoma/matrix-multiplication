\documentclass[a4paper,12pt]{article}
\usepackage{amssymb} % needed for math
\usepackage{amsmath} % needed for math
\usepackage[utf8]{inputenc} % this is needed for umlauts
\usepackage[ngerman]{babel} % this is needed for umlauts
\usepackage[T1]{fontenc}    % this is needed for correct output of umlauts in pdf
\usepackage[margin=2.5cm]{geometry} %layout
\usepackage{fancyhdr}  % needed for the footer
\usepackage{lastpage}  % needed for the footer
\usepackage{hyperref}  % links im text

\usepackage{bchart}
\usepackage{pgfplots}

\begin{document}
\section{Python}
\begin{bchart}[step=5,max=60]
  \bcbar[text=ijk-algorithm]{56}
  \bigskip
  \bcbar[text=ikj-algorithm]{44}
  \bigskip
  \bcbar[label=ikj-algorithm with PyPy 1.3]{10}
  \bigskip
  \bcbar[label=Psyco ikj-algorithm]{6}
  \bigskip
  \bcbar[label=NumPy]{2}
  \bigskip
  \bcbar[label=SciPy]{2}
  \bcxlabel{execution time in minutes}
\end{bchart}

\section{Java}
\begin{bchart}[step=10,max=90]
  \bcbar[text=ijk-algorithm with vectors]{81}
  \bigskip
  \bcbar[text=ijk-algorithm]{27}
  \bigskip
  \bcbar[label=ikj-algorithm]{1}
  \bigskip
  \bcbar[label=JAMA]{0.85}
  \bcxlabel{execution time in minutes}
\end{bchart}

\section{C++}
\begin{bchart}[step=20,max=260]
  \bcbar[label=ijk-algorithm]{27}
  \bigskip
  \bcbar[label=ikj-algorithm]{15}
  \bigskip
  \bcbar[text=Boost]{250}
  \bcxlabel{execution time in seconds}
\end{bchart}

\section{Values for LEAF\_SIZE}
\subsubsection{logarithmic scale}
    \begin{tikzpicture}
       \begin{axis}[
            width=15cm, height=8cm, % size of the image
            grid = major,           
            grid style={dashed, gray!30},
            %xmode=log,log basis x=2,
            %ymode=log,log basis y=2,
            ymin=0, 
            ylabel=time in seconds,
            xlabel=leaf size]

            \addplot table [id=exp]{LEAF_SIZE.csv};
            %\addplot[smooth,samples=200,domain=2:130,thick,red] {600/(x)+15}; 
            \legend{%
                2000 $\times$ 2000 matrix,
                $\frac{600}{x}+15$
            }
        \end{axis}
    \end{tikzpicture}

\subsubsection{logarithmic scale}
    \begin{tikzpicture}
       \begin{axis}[
            width=15cm, height=8cm, % size of the image
            grid = major,           
            grid style={dashed, gray!30},
            xmode=log,log basis x=2,
            ymode=log,log basis y=10,
            ymin=0, 
            ylabel=time in seconds,
            xlabel=leaf size]

            \addplot table [id=exp]{LEAF_SIZE.csv};
            \addplot[smooth,samples=200,domain=2:130,thick,red]   {750}; 
            \addplot[smooth,samples=200,domain=2:130,thick,green] {197}; 
            \addplot[smooth,samples=200,domain=2:130,thick,orange] {66}; 
            \addplot[smooth,samples=200,domain=2:130,thick,lime]   {30}; 
            \addplot[smooth,samples=200,domain=2:130,thick,purple] {19}; 
            \legend{%
                2000 $\times$ 2000 matrix,
                $750$ seconds,
                $197$ seconds,
                $66$ seconds,
                $30$ seconds,
                $19$ seconds
            }
        \end{axis}
    \end{tikzpicture}
\end{document}
